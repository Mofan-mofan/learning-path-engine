# 核心 Schema 设计（v2.0）
## —— 三项核心决策的修订

> 本文在 v0.1 基础上，根据讨论确定的三项核心决策进行了修订。未变更部分参见 `02-核心Schema设计-IC试填版.md`。

---

## 一、v2.0 变更说明

| 项 | v0.1 | v2.0 |
|---|---|---|
| soft 依赖 | 建议进图不阻塞 | ✅ 确认：进图不阻塞，明标"可跳过"；hard 管可行性，soft 管偏好 |
| 知识点粒度锚 | 一条练习能判定 | ✅ 确认：数据驱动，从学习社区（知乎/CSDN/博客等）提取自然粒度 |
| 成果级别与节点掌握 | 只要求直接用到的节点 | ✅ 确认：概率性、非确定性；节点是诊断工具，成果是评估基准 |

---

## 二、soft 依赖：进图不阻塞，明标可跳过

**结论**：
- soft 依赖进图，明标"可跳过"。
- hard 依赖定义学习顺序的可行性约束（拓扑约束，不可违反）。
- soft 依赖定义推荐顺序的偏好（同层内推荐先后，可跳过）。
- 实现：路径排序算法采用两级目标——hard 边定义可行性，soft 边只做偏好。

**自检信号**：如果一个领域填完几乎找不到 soft 边，多半是粒度太粗。

---

## 三、知识点粒度锚：数据驱动

**v0.1 的锚**："一条练习能判定"（专家直觉）。

**v2.0 的锚**：从学习社区的实际发帖中提取自然粒度。

**方法**：
1. 抓取该领域的学习社区内容（知乎、CSDN、博客、MOOC 大纲等）。
2. 分析社区内容中知识点的自然划分：
   - 哪些概念被当作"原子单元"讲解（不再细分）？
   - 哪些概念被频繁拆分为子概念？
   - 社区推荐的练习/题目粒度是什么？
3. 用聚类/统计分析，提取社区共识的粒度。
4. 将社区共识作为 Schema 填表的参考锚点。

**为什么**：
- 社区内容反映实际学习者的认知粒度（"大家怎么学"比"专家怎么想"更贴近真实学习过程）。
- 大数据算力可以处理海量社区内容，找到共识。
- 粒度可以随时间演化（社区认知变化 → 锚点更新）。

**注意**：
- 数据驱动不等于完全自动化——专家仍需根据社区共识调整。
- 不同领域的社区成熟度不同，数据质量有差异。

---

## 四、成果级别与节点掌握：概率性、非确定性

**v0.1 的建议**："只要求直接用到的节点"。

**v2.0 的修订**：节点掌握与成果成功**强正相关，但不绝对**。

### 核心观点

1. **节点掌握与成果成功强正相关，但不绝对**。
   - 掌握更多节点 → 更高概率取得更好成果。
   - 但即使节点不全，也可能完成成果（通过其他路径、天赋、经验等）。

2. **成果是评估基准，节点是诊断工具**。
   - 成果级别由 Rubric 直接判定（可观察证据），不依赖节点掌握。
   - 节点掌握用于诊断"为什么没达到 L4"，帮助还没到 L4 的用户找到下一步学什么。

3. **节点是"复制成功模式"的指标**。
   - 引擎观察：达到 L4 的用户通常掌握了哪些节点。
   - 对于还没到 L4 的用户，引擎推荐这些节点作为"成功模式"的近似。
   - 但这只是推荐，不是要求——用户可以跳过，直接尝试成果。

### 实现语义

- **要求方向**：不做硬门槛。路径排序保证 N_k（某级别的节点足迹）的 hard 闭包排在该级别练习之前（顺序建议），但允许跳过尝试。
- **证据方向**：达到 L_k 作为 N_k 节点的正向证据，更新 `mastery_prob`。
- **implied_prerequisites 减去 ∪N_k 的部分** = "论文默认、但你这一级用不到"的背景，只参与并集→主轴的判断，不做级别门槛。

### 与 hard/soft 依赖的一致性

- N_k 内的依赖按 hard 闭包对待（学习顺序约束）。
- N_k 之外的隐含前置天然表现为 soft（推荐不阻塞）。
- 若两处不一致，会出现"某节点既不要求、又阻塞你"的矛盾——必须避免。

---

## 五、待定项状态（v2.0）

1. ✅ **soft 依赖**：已确认（进图不阻塞，明标可跳过，hard 管可行性，soft 管偏好）。
2. ✅ **知识点粒度锚**：已确认（数据驱动，从学习社区提取自然粒度）。
3. ✅ **成果级别与节点掌握的关系**：已确认（概率性、非确定性；节点是诊断工具，成果是评估基准）。
4. ✅ **并集阈值**：AI 先设经验值，领域间仅小幅差别，不做重点。
5. ✅ **用户提交证据的权重**：暂不做重点。

**当前重心**：聚焦"某一领域知识的学习路径"研究本身（而非平台/通用机制）。

---

## 六、下一步

- [ ] 按 v2.0 口径，从学习社区抓取 IC 器件方向的学习内容，提取粒度锚。
- [ ] 基于社区共识，填 IC 器件方向 30–50 个节点。
- [ ] 从中选 1 个成果实例 + 4 条练习，跑最小闭环验证。
- [ ] 验证：节点掌握与成果成功的正相关性是否成立；诊断是否有效。
