# 通用知识学习与反馈路径引擎 —— 设计思路整理

> 本文只收录交流中经过讨论、被认为更站得住的思路；被否决或降级的部分单列，避免后续误用。

---

## 一、一句话定义

一个**领域无关的学习路径引擎**：用一条**统一固定的学习路径让所有人走**，由 AI 在后台从学习行为中**持续推断能力画像**，并据此跨领域做方向/岗位推荐。

它不以"做题测试"为核心，而以"路径 + 行为 + 成果"三者构成的自适应闭环为核心。

---

## 二、核心思路的演进：什么被否决，什么被确认

### 1. 被否决：一次性"通用能力测试"

**原始想法**：学习前先做一张通用测试卷，测出理解力、记忆力、反推能力、实践能力等，再由能力组合生成学习路径。

**为什么否决**：

- "理解力""反推能力"是日常概念，不是心理学上可测量的构念，没有公认量表；
- 能力高度**领域相关**（数学里的反推强 ≠ 法律条文里的反推强），"通用测试"很难跨领域迁移；
- 单次测试**信度低**，状态、紧张、题目运气都会干扰，用它决定数月学习顺序会把误差放大；
- "能力组合 → 教法/路径"的映射没有可靠证据（教育研究中的 ATI 交互效应几十年反复尝试、极难泛化）。

### 2. 被确认：路径即评估

**修正后的做法**：取消前置测试卷，改为 AI 从学习过程中的**实际表现**推断能力画像（学得多快、在哪卡住、实践能否迁移……）。

**关键洞察**：当路径对所有人是**统一固定**的，这条路径本身就成了**一把尺子**——走法的差异（卡点、速度、迁移能力）就是信号。

- 既保留了"统一路径"的简单，
- 又拿到了个性化画像，
- 且不依赖那张站不住的测试卷。

### 3. 被确认：以结果为导向（逆向设计）

**做法**：先定终点的**可交付成果**，再反向推导所需知识点与练习。

**依据**：这就是课程设计中的 **Backward Design / Understanding by Design（Wiggins & McTighe）**，是被验证过的主流方法，不是野路子。

---

## 三、引擎的四个部件

| 部件 | 职责 |
|---|---|
| **领域本体（Schema）** | 定义什么是"知识点""前置依赖""实践任务""反馈"，是引擎通用的地基 |
| **路径图（DAG）** | 知识点与实践交替的拓扑排序 |
| **练习与判分标准（Rubric）** | 每个成果"合格长什么样"的定义 |
| **学习者模型** | 从行为中**推断**能力画像（隐变量，非声明值） |

> 价值与难点几乎全在 **Schema**，不在代码。代码部分是标准的；Schema 抽对了才通吃，抽错了就退化成"每个领域各写一套"。

---

## 四、被确认的关键原则

### 原则 1：通用原语是"终端成果类型 + Rubric"，不是"知识点"

- "知识点"在数学、法律、芯片、金融里抽象不到同一层；
- "一类可交付成果"可以对齐——它的定义天然含"合格长什么样"和"需要什么前置能力"。
- **知识点是从成果倒推出来的，不是原语。**

### 原则 2：终端成果收敛为少数几种原型

| 原型 | 例子 | 判什么 |
|---|---|---|
| **理解型** | 读懂一篇论文 | 能否复述/复现/批判/推进 |
| **生产型** | 财务模型、商业报告 | 成品质量 |
| **操作型** | 写代码、诊断病症、做出一道菜 | 过程正确性 |

> 把成果原型收敛成少数几类，是引擎能否通用的**真正关键**。

### 原则 3：主轴（固定）+ 分支（按成果倒推）

解决"既要统一固定路径，又要以结果为导向反推"的矛盾：

- **主轴（固定、有序、领域无关地通用）**：基础概念层，所有人一样，慢慢点亮；
- **分支（按成果倒推、随用随取）**：由论文/模型等终端成果反推出的一组组知识点，缺哪个补哪个。

### 原则 4：理解型成果必须分级，否则评估会塌

"读懂论文"不是二元判断，至少四级：

**能复述结论 → 能复现推导 → 能批判出局限 → 能提出改进**

没有分级，就无法判断学习者到底到哪一级。

### 原则 5：前置知识要从"一批成果"取并集

- 单篇论文会**隐藏**它默认的前置知识（半导体物理、工艺、EDA 工具……）；
- 只从**一篇**反推 → 得到"刚好够读这篇"的矮路径，而非系统地基；
- 正确做法：从**一批**论文反推，取它们共同假设的前置概念**并集**——被反复默认的东西，才是基础层。

### 原则 6：用户主动提交证据，加权进画像

- 用户可主动提交已掌握的知识、实践水平、作品、课程与分数、证件等；
- AI 对这些证据**加权**，使"有背景的用户"画像尽量接近其真实水平；
- 前提假设：用户对自己负责，基本不会隐瞒。

### 原则 7：推荐只作参考，不包揽用户想法

- 依据"当前领域较适合或成功人物的能力/性格"做推荐；
- 定位是**给方向**，最终选择权在用户。

---

## 五、明确降级或排除的部分

- **跨领域"能力 → 岗位"推荐**：是整套设计里最弱的一环，映射本身不牢靠；
- **照搬"成功人物的能力和性格"**：存在**幸存者偏差 + 选择偏差**，容易把某领域的筛选结果误当成功条件，反过来固化偏见，需格外当心；
- **推荐效果的后期跟踪反馈**：现阶段**不做**，干扰因素太多，投入产出比低；
- **技术方向的快速替代问题**：由**用户自己**判断，引擎只提供大方向的基本背景。

---

## 六、验收标准

**一个领域专家（例如 IC 专业的研究生），能否不写一行代码，就把他所在领域的路径图填进你的 Schema。**

- 填得进去 → 抽象成立；
- 填不进去 → 回去改原语。

---

## 七、第一个领域的选择

**硬标准：该领域的终端成果是否"开放且充足"。**

| 领域 | 终端成果 | 结论 |
|---|---|---|
| **集成电路（你的专业）** | 论文 | 成果公开且海量，可直接建库、建 Rubric、建练习，无需授权 → **优先** |
| **金融（你感兴趣的方向）** | 财务模型、商业报告 | 多为机构内部资产，公开样本少，需先解决"从哪弄到大量真实成品" → **较难** |

> IC 选得对，理由不止"你懂"，更因为它的终端成果开放可获取。

---

## 八、务实起点（MVP）

1. **不要先做平台**，先在一个领域跑通最小闭环：
   **短诊断 → 生成路径 → 做练习 → 对比产出给反馈 → 更新路径**；
2. 选定一个领域，手工搭一条 **30–50 个节点**的路径；
3. 拉几个人真实使用，验证两件事：
   - 从行为推断出的画像是否**稳定**；
   - 是否**有区分度**（能区分不同水平的人）。
4. Schema 立住了，剩下都是工程量。

> 想作为"公开的思考方式"传播，第一件该产出的不是系统，而是**一份方法论文档 + 一个小而能跑的参考实现**。

---

## 九、待办 / 下一步

- [ ] 确定第一个跑通的领域（IC 论文场景）
- [ ] 草拟核心 Schema：**终端成果 + Rubric + 知识点倒推** 的字段结构
- [ ] 在 IC 领域试填，检验 Schema 的漏洞
- [ ] 定义"理解型成果"四级阶梯的可操作判据
